---
baseline_commit: 7b68cbfbbdf81ee188a1cc38b9e7d2a48e4ac02c
---

# Story 2.2: Outline-First Module & Lesson Structuring

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an Instructor,
I want to add Modules and Lessons to my course as a nested outline, naming a Lesson before writing its content,
so that I can see the whole course take shape before authoring anything.

## Acceptance Criteria

1. **Given** I am in the outline editor for my course, **when** I add a Module, **then** it appears in the outline, ordered after existing Modules, with no content required beyond a name.
2. **Given** I am viewing a Module, **when** I add a Lesson with only a title (no type or content chosen yet), **then** the Lesson is created in an unpublished/incomplete state, and adding it never blocks adding further Lessons.
3. **Given** I am editing outline metadata (Module/Lesson titles, structure), **when** I pause typing for ~600–800ms, **then** the change autosaves automatically with no explicit save button, and the header cycles "Editing…" → "Saved." (announced via `aria-live`).
4. **Given** I close or crash my browser tab mid-edit, **when** I reopen the outline editor, **then** the last-autosaved outline state is intact — nothing typed before the last autosave is lost.
5. **Given** the outline editor is loading, **when** data has not yet resolved, **then** skeleton rows matching the expected Module/Lesson shape are shown, not a blank screen.
6. **Given** I am viewing a Lesson's settings, **when** I check its certificate-eligibility toggle, **then** it defaults to required = true, and I can opt the Lesson *out* of certificate eligibility rather than opting each Lesson in — this value feeds the shared `isCourseComplete()` computation (Epic 4) regardless of Lesson Type.

## Tasks / Subtasks

- [x] Task 1: Course Authoring schema — `module` + `lesson` tables (AC: #1, #2, #6)
  - [x] Extend `lib/modules/course-authoring/schema.ts` (do **not** start a new file — this module already owns Course; it now adds Module and Lesson per AD-2). Follow the exact column/timestamp shape of the existing `course` table in the same file (`id` text PK via `.$defaultFn(() => generateId())`, `timestamptz` for all timestamps, `createdAt`/`updatedAt` with `defaultNow()` + `$onUpdate`).
  - [x] `module` table: `id` (text PK, `generateId()`), `courseId` (text, `.notNull().references(() => course.id, { onDelete: "cascade" })`), `title` (text, `.notNull()`), `position` (`integer("position").notNull()` — see Task 2 for the ordering contract), `removedAt` (`timestamp("removed_at", { withTimezone: true })`, nullable — AD-11 soft-delete; see Dev Notes for why it lands now), `createdAt`/`updatedAt`. Index on `courseId` (`module_course_id_idx`), mirroring `course_instructor_id_idx`.
  - [x] `lesson` table: `id`, `moduleId` (text, `.notNull().references(() => module.id, { onDelete: "cascade" })`), `title` (text, `.notNull()`), `lessonType` (`lessonType("lesson_type")` pg enum — **nullable**; AC #2 requires a Lesson to exist with a title and no type. See Task 3), `position` (`integer`, `.notNull()`), `required` (`boolean("required").notNull().default(true)` — AD-7; feeds `isCourseComplete()` in Epic 4), `removedAt` (nullable `timestamptz`), `createdAt`/`updatedAt`. Index on `moduleId` (`lesson_module_id_idx`).
  - [x] Add Drizzle `relations`: extend `courseRelations` with `modules: many(module)`; add `moduleRelations` (`course: one(course, …)`, `lessons: many(lesson)`) and `lessonRelations` (`module: one(module, …)`). Match the style of the existing `courseRelations` block.
  - [x] No barrel edit needed — `lib/db/schema.ts` already does `export * from "@/lib/modules/course-authoring/schema"`.
  - [x] `npm run db:generate` then `npm run db:migrate` — produces `drizzle/0007_*.sql` (two tables + `lesson_type` enum + FKs + indexes). Apply to the live Neon DB (same flow as Story 2.1's `0005`/`0006`).
- [x] Task 2: Ordering contract — integer `position`, contiguous, 0-based (AC: #1, #2)
  - [x] `position` is a plain `integer`, 0-based, contiguous within a parent (module's position within its course; lesson's position within its module). A newly added Module gets `position = <count of that course's non-removed modules>`; a newly added Lesson gets `position = <count of that module's non-removed lessons>` — i.e. always appended last (AC #1 "ordered after existing Modules"; AC #2 by parallel).
  - [x] `[DECISION]` Not fractional indexing. Drag-reorder (Story 2.3) will rewrite the affected sibling list's `position` values in one pass. Fractional/`fractional-indexing` keys were considered and rejected: their payoff is conflict-free concurrent reordering, which AD-4 explicitly defers for v1 (no multi-Instructor co-authoring of one Course), and an outline is small-N (tens of rows). Cheap to revisit inside Story 2.3 if that proves wrong — it does not change this story's surface.
  - [x] Every outline read in this story filters `removedAt IS NULL` and orders by `position` asc. Establish that filter now so Stories 2.3 / 2.11 / Epic 4 inherit it rather than retrofit it.
- [x] Task 3: `lesson_type` enum — column exists now, picker does not (AC: #2)
  - [x] Add `export const lessonType = pgEnum("lesson_type", LESSON_TYPES)` to `schema.ts`, with `LESSON_TYPES` defined as an import-free `as const` tuple in `lib/modules/course-authoring/course.ts` (same pattern as `COURSE_CATEGORIES` / `COURSE_CONTENT_LANGUAGES` already there): `["video", "text", "pdf", "quiz", "code"]` — the five FR-9 Lesson Types (`code` = Interactive Code Exercise; matches the mockup's `CODE` chip). Export `type LessonType` and an `isLessonType` guard via the existing `memberOf()` factory.
  - [x] This story writes **no UI to set `lessonType`** — the per-type pickers are Stories 2.4–2.8. The column is nullable and lands now because (a) AD-4 names "Lesson Type" as part of autosave field-group (a) "outline metadata", which is exactly the field-group this story's autosave owns, and (b) it avoids a schema migration mid-Epic. A Lesson with `lessonType = null` renders as "Not started" in the outline (Task 6).
- [x] Task 4: Service layer — outline read + structural writes (AC: #1, #2, #3, #6)
  - [x] Extend `lib/modules/course-authoring/service.ts` (siblings of the existing `createCourse` / `listCoursesByInstructor` / `getCourseById` — plain functions, direct Drizzle queries, return values/`null`, **no `{ok,…}` wrapping** — that is the action-layer convention). Authorization stays out of the service (it lives in `actions.ts`).
  - [x] `getCourseOutline(courseId): Promise<CourseOutline>` — one query for the course's non-removed `module` rows ordered by `position`, one query for the non-removed `lesson` rows whose `moduleId` is in that set (ordered by `position`), stitched into `{ modules: Array<{ id, title, position, lessons: Array<{ id, title, lessonType, required, position }> }> }`. Project only those columns (the `courseListColumns` precedent). Two queries + in-memory stitch is fine — do not pull full rows.
  - [x] `addModule({ courseId, title }): Promise<ModuleRow>` — computes append `position` (Task 2) and inserts. `title` is pre-trimmed/validated by the action. Returns the created row (the client needs its `id`).
  - [x] `addLesson({ moduleId, title }): Promise<LessonRow>` — same, `position` among the module's non-removed lessons. Returns the created row.
  - [x] `updateModuleTitle({ moduleId, title }): Promise<ModuleRow | null>` and `updateLessonTitle({ lessonId, title }): Promise<LessonRow | null>` — the autosave writes for field-group (a). Last-write-wins (AD-4): a plain `UPDATE … SET title = …` (Drizzle's `$onUpdate` bumps `updatedAt`). Return `null` when the row does not exist or is already `removedAt`-flagged.
  - [x] `setLessonRequired({ lessonId, required }): Promise<LessonRow | null>` — the AC #6 toggle write.
  - [x] Ownership resolution helpers so the action layer can authorize a Module/Lesson mutation against the owning Instructor without a raw cross-table query in `actions.ts`: `getModuleCourseContext(moduleId): Promise<{ courseId: string; instructorId: string } | null>` and `getLessonCourseContext(lessonId): Promise<{ courseId: string; moduleId: string; instructorId: string } | null>` (join `lesson → module → course`, or two small reads). One shape, reused by every Module/Lesson action here and in Stories 2.3–2.8.
- [x] Task 5: Server Actions — instructor + ownership guarded (AC: #1, #2, #3, #6)
  - [x] Extend `lib/modules/course-authoring/actions.ts` (`"use server"` already at top). Every new action: `await requireRole("instructor")` first, then resolve ownership via the Task 4 context helper + `isOwner(user, ctx.instructorId)` (`lib/auth/authorization.ts` — the AD-6 resource primitive Story 2.1 added; do **not** inline `=== user.id`), then the whole body in `try/catch` with `unstable_rethrow(error)` first, returning `ActionResult<T>` from `@/lib/actions/result`. Mirror the existing `createCourseAction` exactly (auth failure → `{ ok:false, error:{ code:"forbidden", … } }`; not-found → `{ code:"not_found" }`).
  - [x] `addModuleAction(courseId: string, title?: string): ActionResult<{ module: OutlineModule }>` — validates/defaults the title (an empty add is allowed — default to a localized "Untitled module" placeholder value, or reject empty and require the client to send the placeholder; dev's call, but AC #1 says "no content required beyond a name", so a name must exist — prefer: client always sends a non-empty default). Returns the new module (empty `lessons`).
  - [x] `addLessonAction(moduleId: string, title?: string): ActionResult<{ lesson: OutlineLesson }>` — same shape. **Must return fast and not redirect / not force navigation** (AC #2 "adding it never blocks adding further Lessons").
  - [x] `renameModuleAction(moduleId: string, title: string): ActionResult<{ updatedAt: string }>` and `renameLessonAction(lessonId: string, title: string): ActionResult<{ updatedAt: string }>` — the debounced autosave endpoints. Trim server-side; reject empty title with `{ code:"title_required" }` (the client keeps the last good value and shows the outline-level save-error state — see Task 7). Return the new `updatedAt` so the client can hold AD-4's optimistic version.
  - [x] `setLessonRequiredAction(lessonId: string, required: boolean): ActionResult<{ required: boolean }>`.
  - [x] Reuse the shared `parseCourseFields`-style approach for title bounds: add `LESSON_TITLE_MAX_LENGTH` / `MODULE_TITLE_MAX_LENGTH` to `course.ts` (mirror `COURSE_TITLE_MAX_LENGTH = 120`) and a tiny `parseOutlineTitle(raw): { ok:true, value } | { ok:false, error }` helper there, called by both the actions and the client (same client+server single-source rule Story 2.1 established with `parseCourseFields`).
  - [x] Autosave endpoints do **not** call `revalidatePath` (it would thrash the RSC payload on every pause and fight the client's optimistic outline). `addModuleAction` / `addLessonAction` may optionally `revalidatePath("/[locale]/courses", "page")` so "My Courses" lesson counts (if any are later shown) stay fresh — not required for this story.
- [x] Task 6: Outline editor UI — grow the existing course-builder route (AC: #1, #2, #3, #5, #6)
  - [x] `app/[locale]/(instructor)/courses/[courseId]/page.tsx` — this is the route Story 2.1 explicitly reserved for the outline editor ("Story 2.2 grows this **same route** into the outline editor" — 2.1 Dev Notes & Completion Notes). Keep its existing guards (role via `can()` → `notFound()`, then `isOwner(user, course.instructorId)` → `notFound()`). Replace the `builder.outlineComingSoon` line with the editor: fetch `getCourseOutline(courseId)` and render a new client component `<OutlineEditor courseId={course.id} courseTitle={course.title} initialOutline={outline} />`.
  - [x] New `app/[locale]/(instructor)/courses/[courseId]/loading.tsx` — Next 16 route-level loading UI; renders skeleton module/lesson rows immediately on navigation while the server component awaits (AC #5). Use a `Skeleton` primitive — add it via `npx shadcn@latest add skeleton` (base-ui, consistent with Story 2.1's `select` addition) or a 6-line `components/ui/skeleton.tsx` if the generated one is heavier than needed. Skeleton shape: 2–3 module header bars, each with 2–3 indented lesson-row bars. Give the wrapper an `aria-busy="true"` + a visually-hidden localized "Loading outline…" label.
  - [x] New client components (co-located under the route or `components/course/outline/`): `outline-editor.tsx` (owns the outline state + autosave status), `module-block.tsx`, `lesson-row.tsx`. Keep them scoped to this feature — not a general tree-editor abstraction.
  - [x] Editor header bar: breadcrumb `My Courses /` (localized, `Link` to `/courses`), the course title, the `Instructor` role badge (reuse the `Badge` component / `secondary` treatment), and the autosave status region (Task 7). Render **disabled** "Preview as learner" and "Publish" buttons (accessible name + `disabled`; Stories 2.9 and 2.10 wire them) so the hero surface matches the mockup — or omit them if cleaner; do not build their behavior here.
  - [x] **Do not** add an in-editor language toggle — the global `SiteHeader` language switcher (Story 1.6) already covers every surface; the mockup's in-editor toggle is illustrative, not a second control to build.
  - [x] Body: for each module — a module block with an **inline editable title** (a text input styled as a heading, not a separate "edit" mode; autosaves on pause) and a `+ Add lesson` button. Under it, its lesson rows: each an **inline editable title** input, a small meta label (`lessonType` label if set, else localized "Not started"), and the AC #6 certificate toggle (Task 8). A `+ Add module` button sits after the last module.
  - [x] **No drag handles, no reorder arrows, no move up/down controls** — reordering (drag + its keyboard equivalent) is entirely Story 2.3. This story delivers structure + naming + autosave only. Rows render in `position` order.
  - [x] Empty state: a course with zero modules shows an inviting localized "Add your first module" prompt with the add-module action, not a blank editor (house style, UX-DR15 family — keep it minimal).
  - [x] Adding a module/lesson: call the action, append the returned row to local state, keep the new title input focused/selectable so the Instructor can immediately type a name and immediately add another (AC #1, AC #2 "never blocks adding further Lessons"). Optimistic-append is acceptable; reconcile on the action result and roll back with a save-error state on failure.
- [x] Task 7: Autosave mechanics + status (AC: #3, #4)
  - [x] A small `useOutlineAutosave` hook (or inline logic in `outline-editor.tsx`) — scoped to this feature, in the spirit of `useLocaleSwitchFormState` (not a generic "autosave anything"). Per edited field: hold the dirty value, debounce **700ms** (within AC #3's 600–800ms), then dispatch the rename action. Per AD-4: each field-group tracks its own `updatedAt`-based optimistic version; last-write-wins; never a whole-document overwrite. The only field-group this story edits is outline metadata (title) — one debounced call per module/lesson title.
  - [x] Status machine surfaced in the header, one shared indicator: `idle` (no text or a resting "Saved.") → `editing` ("Editing…", set on the first keystroke after idle) → `saving` (debounce fired, action pending — may keep showing "Editing…") → `saved` ("Saved.") on success → `error` on failure (a localized "Couldn't save — check your connection" with a manual "Retry" that re-dispatches the pending write; never a silent stall, never a dead end — EXPERIENCE.md Voice/Tone).
  - [x] Wrap the indicator text in `aria-live="polite"` (+ `role="status"`) so "Editing…" / "Saved." are announced (AC #3, UX-DR11). Do not announce on every keystroke — announce on transition into `editing` and into `saved`/`error`.
  - [x] Concurrency: the client dispatches Server Actions one at a time (documented Next.js behavior). Debounce-coalesce rapid edits to the *same* field so only the latest value is sent; a queued newer edit supersedes an in-flight older one for that field.
  - [x] AC #4 (crash recovery) is satisfied **structurally** by the debounced server write — on reopen, the server component re-fetches `getCourseOutline` and the last saved state renders. **Do not** build a `localStorage`/`sessionStorage` draft layer: AC #4 explicitly accepts losing only what was typed in the sub-second before the crash ("nothing typed before the last autosave is lost"). `[DECISION]` Also do not wire `useLocaleSwitchFormState` into the editor — a mid-typed unsaved title lost on a locale switch is the same conscious exception Story 2.1 took for its creation form (that AC belonged to Story 1.6's auth forms); autosave-on-pause already keeps the window tiny.
- [x] Task 8: Certificate-eligibility toggle (AC: #6)
  - [x] On each lesson row, an inline toggle (a real `<input type="checkbox">` / shadcn-style switch with a visible + accessible label, e.g. "Counts toward certificate"), **default checked** (`required = true`). Unchecking opts the Lesson out. Calls `setLessonRequiredAction(lessonId, next)` immediately (not debounced — it is a discrete setting, not typed text; AD-4's debounce contract is about the two editor field-groups). Optimistic flip, roll back + surface the save-error state on failure.
  - [x] `[NOTE for dev]` A fuller per-Lesson "settings" panel arrives with the type editors (2.4–2.8). This story surfaces only this one toggle because AC #6 requires it now and Epic 4's `isCourseComplete()` depends on the `required` column being real and Instructor-controllable from day one, for every Lesson Type.
- [x] Task 9: i18n + accessibility (AC: all)
  - [x] Extend the `Course` namespace in `lib/i18n/en.json` **and** `lib/i18n/ar.json` (identical structure — EN/AR parity is enforced) with an `outline.*` group: header (`breadcrumb`, `previewAsLearner`, `publish`), autosave (`editing`, `saved`, `saveError`, `retry`), structure (`addModule`, `addFirstModule`, `addLesson`, `moduleTitlePlaceholder`, `lessonTitlePlaceholder`, `untitledModule`, `untitledLesson`), lesson meta (`notStarted`, and `lessonType.video|text|pdf|quiz|code` labels), `requiredToggleLabel`, `loadingOutline` (skeleton aria-label). No hardcoded user-facing strings anywhere in the new components.
  - [x] Any count shown (e.g. an optional "N lessons" on a module) goes through `formatNumber` from `lib/i18n/format.ts` — never bare `toLocaleString`, so digits stay Western Arabic (0–9) under `ar` (UX-DR5, Consistency Conventions).
  - [x] Accessibility bar (same as every Epic 1–2 story): CSS **logical properties only** (no `left`/`right`); every inline title input has a `<label>` (visually-hidden is fine) or `aria-label`; `+ Add …` are real buttons with accessible names; the required toggle exposes its state; focus order follows logical reading order in both directions; interactive targets ≥ ~44px (the `h-11` floor Story 2.1 set on `select`); errors use the shared `FormMessage` (icon + text, never colour alone — UX-DR9).
- [x] Task 10: End-to-end verification (AC: all)
  - [x] Open `/courses/{courseId}` as the course's Instructor → the outline editor renders; a course with no modules shows the "Add your first module" prompt.
  - [x] Add a Module → appears last, name it, add a second → ordered after the first (AC #1). Add a Lesson under a module with only a title → row appears, `lessonType` null → shows "Not started"; immediately add another Lesson without any navigation or block (AC #2).
  - [x] Edit a Module title and a Lesson title; stop typing → within ~700ms the header shows "Editing…" then "Saved."; confirm the `aria-live` region announces both (AC #3). Confirm `module.title` / `lesson.title` and `updated_at` changed in the DB.
  - [x] Hard-reload the page after "Saved." → edits persist (AC #4). Type a change and reload **before** the debounce fires → the pre-last-save value is shown (accepted by AC #4).
  - [x] Throttle/kill the network, edit a title → the status goes to the error state with a working Retry, not a silent stall.
  - [x] Navigate to the route and observe `loading.tsx` skeleton rows before data resolves (AC #5) — throttle network to see it.
  - [x] Toggle a Lesson's certificate-eligibility off and on → `lesson.required` flips in the DB; default of a newly added Lesson is `true` (AC #6).
  - [x] As a **different** Instructor and as a plain Learner: `/courses/{courseId}` 404s; direct calls to the new Server Actions with another Instructor's `moduleId`/`lessonId` return `{ ok:false, error:{ code:"forbidden" } }` (ownership, not just role).
  - [x] DB-layer check script (throwaway, deleted after — the Story 2.1 pattern): module/lesson insert, `position` append correctness, `removed_at` defaults null, `required` defaults true, cascade delete (course→module→lesson), `lesson_type` enum rejects a bad value, `getCourseOutline` returns ordered/stitched shape and excludes `removed_at`-flagged rows. Leave the DB clean.
  - [x] `npm run build`, `npx tsc --noEmit`, `npm run lint` all clean; spot-check `/en` and `/ar` (RTL: outline indentation, inline inputs, toggle, and skeleton all mirror; no `left`/`right` leakage).
  - [x] `[KNOWN ENV LIMITATION]` Story 2.1 found the in-app browser pane does not reliably dispatch synthetic click/type events. If interactive click-through can't be driven here, verify server/DB behavior directly (script + `getCourseOutline` reads) and note that a manual local walkthrough + the `code-review` workflow are the interaction gates.

## Dev Notes

- **This story adds the 2nd and 3rd tables to Course Authoring — `module` and `lesson` — extending, not restructuring, the slice Story 2.1 started.** `schema.ts`, `service.ts`, `actions.ts` all already exist with `course` in them; add to each, matching the shape that is already there (UUIDv7 `.$defaultFn`, `timestamptz`, `defaultNow()`/`$onUpdate`, `pgEnum` from an import-free `course.ts` tuple, per-FK index). Do not create `module.ts` / `lesson.ts` files — one slice per module (AD-2), the `course` table's own file is that slice.
  [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-2, #Consistency-Conventions]
- **AD-4 defines the autosave contract this story implements — and bounds its scope.** Field-group (a) is "outline metadata (title, order, Lesson Type)"; field-group (b) is "content body". This story only touches (a), and only its `title` sub-part (order = Story 2.3, Lesson Type picker = Stories 2.4–2.8). Each field-group is "its own debounced Server Action call and its own `updatedAt`-based optimistic version… last-write-wins per field-group, never whole-document overwrite." Build the autosave that way even though only one field-group is live now, so 2.4+ slot in without reworking it.
  [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-4]
- **`lesson.required` (AC #6) is an AD-7 contract input, not a cosmetic setting.** Every Lesson row carries `required: boolean` default `true` "regardless of Lesson Type" so Learning Experience's single `isCourseComplete()` has a stable input. It must be a real column and Instructor-controllable now — Epic 4 reads it, it is not added later. AD-7 also fixes the default direction: opt a Lesson *out*, never opt each one *in*.
  [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-7]
- **`removedAt` lands on `module` and `lesson` now, on purpose.** AD-11 makes every Course/Module/Lesson removal a soft-delete, and binds Course Authoring's "ordinary post-publish editing, not just Admin actions" (Story 2.11). This story has no delete UI, but every outline read it writes (`getCourseOutline`, the ordering counts) must filter `removedAt IS NULL` — establishing that column and filter now means Stories 2.3 / 2.11 and Epic 4's `isCourseComplete()` inherit a correct base instead of a migration + a query audit later. This is the same "build the bounded thing now rather than retrofit" call Story 2.1 made for the category enum.
  [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-11]
- **Ordering is a plain 0-based contiguous `integer position`, appended on add; drag-reorder (Story 2.3) rewrites sibling positions in one pass.** Fractional indexing was weighed and rejected — its only real advantage is conflict-free concurrent reordering, which AD-4 explicitly defers for v1 (Sanabel has no multi-Instructor co-authoring), and the outline is small-N. [Source: web research 2026 — fractional vs. integer reordering trade-offs.]
- **This route already exists — grow it, don't replace it.** `app/[locale]/(instructor)/courses/[courseId]/page.tsx` was built by Story 2.1 as "the minimal-but-real course-builder landing page it [2.2] will grow from (same route, not a throwaway placeholder)." Keep its guards verbatim (`can("instructor")` → `notFound()`, then `isOwner(user, course.instructorId)` → `notFound()`), keep the title/description/badges header if it fits the layout, and replace the `builder.outlineComingSoon` string with the editor.
  [Source: _bmad-output/implementation-artifacts/2-1-low-friction-course-creation.md#Dev-Notes, #Completion-Notes-List]
- **Reuse, do not reinvent:** `isOwner()` (`lib/auth/authorization.ts`) for every Module/Lesson ownership check — never an inline `=== user.id`; `ActionResult<T>` + `actionErrorText()` (`lib/actions/result.ts`) for every action + its client error mapping; `requireRole("instructor")` in actions, `can()`→`notFound()` in the page (the AD-6 split `(instructor)/courses/page.tsx` documents in its own comment); `parseCourseFields`-style shared client+server validation in `course.ts` for the new title bounds; `FormMessage` (icon+text) for errors; `Badge` for the role tag; `formatNumber` for any count; the `Field`/inline-`Input` + `Label` primitives; `Link`/`useRouter` from `lib/i18n/navigation`. The global `SiteHeader` already provides the language switcher on this surface — do not add a second one.
  [Source: _bmad-output/implementation-artifacts/2-1-low-friction-course-creation.md#File-List; _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-6]
- **Next.js 16 specifics for this story:** Server Actions are POST-only and reachable directly — every new action re-checks `requireRole` + ownership regardless of the page guard. The client dispatches actions **one at a time** (documented; may change) — coalesce per-field debounced writes rather than firing one per keystroke. Route-level `loading.tsx` is the idiomatic way to get AC #5's instant skeleton on navigation. If a post-mutation refresh is ever needed use `revalidatePath`/`revalidateTag` or `refresh()` from `next/cache` — but autosave writes here deliberately skip all of them (they would thrash the RSC payload and fight the client's optimistic outline).
  [Source: node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md]
- **UX intent:** [mockups/key-outline-editor.html] is the visual contract for header + nesting + autosave "Saved." Note the mockup also shows drag handles, reorder arrows, an in-editor language toggle, and upload status rows — **none of those are this story** (drag = 2.3, upload = 2.4/2.6, language toggle = global already). Autosave UX: no save button anywhere, save on pause, header cycles "Editing…" → "Saved." as text (no modal/toast), announced via `aria-live` (UX-DR12). Skeleton on load, not a blank screen (State Patterns / AC #5).
  [Source: _bmad-output/planning-artifacts/ux-designs/ux-BMAD Test-2026-08-02/EXPERIENCE.md#Component-Patterns, #State-Patterns, #Accessibility-Floor; DESIGN.md#Layout-Spacing, #Components]
- **No dedicated test framework is pinned** (same as every prior story) — verification is the Task 10 AC walkthrough + a throwaway DB-layer script + `build`/`tsc`/`lint` + `/en` & `/ar` spot-check.

### Project Structure Notes

- All new/edited files sit inside the existing structure — no new top-level directories:
  - `lib/modules/course-authoring/schema.ts` (edit — add `module`, `lesson`, `lessonType` enum, relations) + new `drizzle/0007_*.sql` migration (+ `drizzle/meta/*` updates)
  - `lib/modules/course-authoring/course.ts` (edit — `LESSON_TYPES` + `isLessonType`, `MODULE_TITLE_MAX_LENGTH` / `LESSON_TITLE_MAX_LENGTH`, `parseOutlineTitle`)
  - `lib/modules/course-authoring/service.ts` (edit — `getCourseOutline`, `addModule`, `addLesson`, `updateModuleTitle`, `updateLessonTitle`, `setLessonRequired`, `getModuleCourseContext`, `getLessonCourseContext` + their types)
  - `lib/modules/course-authoring/actions.ts` (edit — `addModuleAction`, `addLessonAction`, `renameModuleAction`, `renameLessonAction`, `setLessonRequiredAction`)
  - `app/[locale]/(instructor)/courses/[courseId]/page.tsx` (edit — render the outline editor)
  - `app/[locale]/(instructor)/courses/[courseId]/loading.tsx` (new — skeleton)
  - `components/course/outline/outline-editor.tsx`, `module-block.tsx`, `lesson-row.tsx` (new — or co-located under the route) + an optional `use-outline-autosave.ts` hook
  - `components/ui/skeleton.tsx` (new — `npx shadcn@latest add skeleton`, or hand-rolled)
  - `lib/i18n/en.json`, `lib/i18n/ar.json` (edit — `Course.outline.*`)
- No conflicts expected: `course-authoring` gains two tables via one additive migration; the `[courseId]` route is fleshed out in place; `(instructor)` route-group guards are unchanged. Mirrors the Accounts module's own incremental growth across Stories 1.3→1.4→1.5.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-2.2, #Epic-2] — story statement, the six acceptance criteria (already BDD-formatted), and Epic 2 cross-story scope (2.3 drag-reorder, 2.4–2.8 per-type authoring, 2.9 preview, 2.10 publish, 2.11 post-publish edits all build on this outline)
- [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/ARCHITECTURE-SPINE.md#AD-1, #AD-2, #AD-3, #AD-4, #AD-6, #AD-7, #AD-9, #AD-11, #Consistency-Conventions] — mutation boundary, module ownership map, cross-module access rule, autosave/concurrency contract, role/authorization helpers, completion/`required` contract, i18n rule, soft-delete contract, UUIDv7/snake_case/`timestamptz`/discriminated-union conventions
- [Source: _bmad-output/planning-artifacts/architecture/architecture-BMAD Test-2026-08-02/SOLUTION-DESIGN.md#3-Module-Architecture, #6-Cross-Cutting-Contracts] — why the module boundary matters, and the "settle the answer once, centrally" rationale behind AD-4/AD-7
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-BMAD Test-2026-08-02/EXPERIENCE.md#Information-Architecture, #Component-Patterns, #State-Patterns, #Interaction-Primitives, #Accessibility-Floor] — "Course outline editor" row (FR-7/8/12), Autosave + Drag-reorder + Upload behavioral rules (only Autosave applies here), skeleton-on-load state, `aria-live` announcements
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-BMAD Test-2026-08-02/DESIGN.md#Layout-Spacing, #Elevation-Depth, #Components] + [mockups/key-outline-editor.html] — logical-property layout, light elevation on dense authoring screens, Badge/Button treatment, autosave "Saved." text pattern; the mockup illustrates header + nesting + autosave (its drag/upload/lang-toggle elements are out of this story's scope)
- [Source: _bmad-output/implementation-artifacts/2-1-low-friction-course-creation.md] — the `course` schema/service/action shape to mirror, `isOwner()` resource primitive, `ActionResult`/`actionErrorText`/`parseCourseFields`/`CourseMetaBadges` reuse, `npx shadcn add` precedent, the "same route grows into the outline editor" reservation, and the in-app-browser synthetic-event limitation
- [Source: _bmad-output/implementation-artifacts/1-6-bilingual-ui-shell-language-switcher.md] — `useLocaleSwitchFormState` and next-intl issue #496 (why a locale switch remounts `app/[locale]` and drops client state) — the conscious reason this story does *not* wire draft-persistence into the editor
- [Source: node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md] — Next.js 16 Server Actions (POST-only, direct-callable, one-at-a-time client dispatch), `revalidatePath`/`revalidateTag`/`refresh()`, pending-state patterns
- [Source: web research, 2026 — "fractional indexing vs integer position for drag-reorder"] — dev.to/steveruiz/hollos.dev: integer `position` with sibling-list rewrite is the right call for small-N, single-author lists; fractional indexing's payoff is concurrent multi-user reorder (deferred by AD-4)
- Current repo state verified directly: `lib/modules/course-authoring/{schema,service,actions,course}.ts` (course only, no module/lesson), `app/[locale]/(instructor)/courses/[courseId]/page.tsx` (minimal landing page + guards), `app/[locale]/(instructor)/layout.tsx`, `lib/auth/authorization.ts` (`isOwner`, `can`, `requireRole`), `lib/actions/result.ts`, `lib/db/{client,id,schema}.ts`, `lib/i18n/{en,ar}.json` (`Course` namespace), `lib/i18n/format.ts`, `components/ui/` (no `skeleton.tsx`), `drizzle/` (latest `0006`), `package.json` (Next 16.2.12, React 19.2.4, drizzle-orm 0.45.2, next-intl 4.13.4, `@base-ui/react`, `db:generate`/`db:migrate` scripts)

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (Claude Code / bmad-dev-story workflow)

### Debug Log References

- `npm run db:generate` → `drizzle/0007_fresh_forge.sql` (`module` + `lesson` tables, `lesson_type` enum, 2 FKs, 2 indexes). `npm run db:migrate` → applied to the live Neon database.
- `npx tsc --noEmit` → clean. `npm run lint` → clean. `npm run build` → clean (`/[locale]/courses/[courseId]` registered, dynamic).
- **Lint caught `module` as a schema export** — `@next/next/no-assign-module-variable`. Renamed the exported const `module` → `courseModule` across `schema.ts` / `service.ts` (the Postgres table name is still `"module"`; migration/DB unchanged).
- Throwaway DB-layer verification script (project root, run via `npx tsx --env-file=.env.local`, then deleted): **32 assertions, all PASS** — append-`position` correctness for Modules and Lessons (incl. per-module scoping and skipping soft-removed rows), `lessonType` null / `required` true / `removedAt` null defaults, `getCourseOutline` ordered+stitched+projected shape, `updateModuleTitle`/`updateLessonTitle` (+ `updatedAt` advance), `setLessonRequired` both ways, `getModuleCourseContext`/`getLessonCourseContext` resolve owner + return `null` for unknown/soft-removed, AD-11 soft-removed Modules/Lessons excluded from `getCourseOutline`, `lesson_type` enum rejects an unknown value, course→module→lesson FK cascade. DB left clean (0 verify rows).
- Dev-server smoke (Turbopack): `GET /en 200`, `GET /en/courses/<fake-id> 404` (guards run — unauthenticated request 404s the Instructor route, no 500), no console/server errors.
- **Interactive UI click-through not run** — same environment limitation Story 2.1 documented (in-app browser doesn't dispatch synthetic events; the only DB user is `am161050@gmail.com` and no password is available to establish an Instructor session). Recommend a manual local walkthrough (add module/lesson, autosave "Editing…"→"Saved.", `loading.tsx` skeleton, certificate toggle, `/ar` RTL) plus the `code-review` workflow (different LLM) as the interaction gates.

### Completion Notes List

- **Schema (Tasks 1–3):** `course-authoring/schema.ts` gains `courseModule` (DB table `module`) and `lesson`, plus the `lesson_type` pg enum (`video|text|pdf|quiz|code`). Both tables mirror `course`'s shape: `text` PK via `generateId()`, `timestamptz` everywhere, `defaultNow()`+`$onUpdate`, one FK index. `module.position` / `lesson.position` are 0-based contiguous `integer` (append-on-add; Story 2.3 owns reorder). `lesson.lessonType` is **nullable** (AC #2 — title-only Lessons); `lesson.required` is `boolean NOT NULL DEFAULT true` (AD-7). `removedAt` (nullable `timestamptz`) is on both now (AD-11) — no removal UI in this story, but every outline read filters `removed_at IS NULL` from day one. Relations added: `course → many module`, `module → one course / many lesson`, `lesson → one module`.
- **`module` → `courseModule` rename:** `module` as a top-level binding trips Next's `no-assign-module-variable` lint rule, so the exported schema const is `courseModule`. `pgTable("module", …)` keeps the DB table/column names unchanged; migration `0007` is unaffected. Story 2.3+ should import `courseModule`.
- **Shared types moved to `course.ts`:** `OutlineLesson` / `OutlineModule` / `CourseOutline` are declared in the import-free `course.ts` (not `service.ts`) so the client outline editor imports them without pulling `service.ts`'s `db` into the client bundle. `service.ts` re-exports them for callers that already import from it.
- **Service (Task 4):** `getCourseOutline` (2 queries + in-memory stitch, projected columns, `removedAt`-filtered, `position`-ordered), `addModule` / `addLesson` (append `position` via `max(position)+1` of the live set), `updateModuleTitle` / `updateLessonTitle` (AD-4 field-group (a) last-write-wins), `setLessonRequired` (AC #6), and `getModuleCourseContext` / `getLessonCourseContext` (join to the owning Course + Instructor for the action-layer authz check).
- **Actions (Task 5):** `addModuleAction`, `addLessonAction`, `renameModuleAction`, `renameLessonAction`, `setLessonRequiredAction`. Each: `requireRole("instructor")` → `isOwner()` against the resolved Course Instructor (shared `requireCourseOwner` / `requireModuleOwner` / `requireLessonOwner` helpers) → body in `try/catch` with `unstable_rethrow` first → `ActionResult<T>`. `not_found` vs `forbidden` distinguished. Rename actions return the new `updatedAt` (ISO) for the client's optimistic version. `addModuleAction` calls `revalidatePath("/[locale]/courses", "page")`; the rename/toggle actions deliberately do **not** revalidate (would thrash the RSC payload against the client's optimistic outline).
- **Outline editor (Tasks 6–8):** `app/[locale]/(instructor)/courses/[courseId]/page.tsx` now fetches `getCourseOutline` and renders `<OutlineEditor>` (guards unchanged from Story 2.1). New `loading.tsx` → `<OutlineEditorSkeleton>` (client, pulls its `aria-busy` label from `useTranslations` to avoid request-locale resolution in `loading.tsx`; skeleton module/lesson rows — AC #5). `<OutlineEditor>` owns the outline state + shared autosave status; header = breadcrumb `Link` + course title + Instructor `Badge` + autosave indicator + **disabled** "Preview as learner" / "Publish" (Stories 2.9 / 2.10 wire them); a slim `<CourseMetaBadges>` row keeps category / "Taught in" / Draft visible. No in-editor language toggle (global `SiteHeader` covers it). `<ModuleBlock>` = inline-editable name + `<LessonRow>` list + "Add lesson". `<LessonRow>` = inline-editable title + type/"Not started" meta + native `<input type="checkbox">` "Counts toward certificate" (default checked, optimistic flip + rollback on failure). **No drag handles / reorder controls** — Story 2.3. Empty course → an "Add your first module" prompt card, not a blank editor. After an add, `focusId` autofocuses the new row's title input so the Instructor can name it immediately and add another (AC #1/#2).
- **Autosave (Task 7):** `use-outline-autosave.ts` hook — per-field 700ms debounce, coalesces rapid edits to the same field (newest saver wins), tracks in-flight + failed sets. Status machine `idle → editing → saving → saved | error`, surfaced once in the header inside `role="status" aria-live="polite"` (announces "Editing…" / "Saved." / error). Error state shows a "Retry" that re-runs only the failed field saves. AC #4 (crash recovery) is structural — the debounced write lands in Postgres, the Server Component re-fetches on reopen; **no** `localStorage`/`sessionStorage` draft layer and **no** `useLocaleSwitchFormState` wiring (the same conscious exception Story 2.1 took for its creation form).
- **i18n (Task 9):** new `Course.outline.*` group in `en.json` + `ar.json` (identical structure) — header, autosave status, add actions, placeholders, `untitled*` defaults, `lessonType.*` labels, `requiredToggle`, `loadingOutline`. Arabic uses the app's established `مدرّب` for the Instructor badge (not the mockup's `مدرّس`). Removed the now-unreferenced `Course.builder.outlineComingSoon` key (both catalogs). No hardcoded user-facing strings; logical properties throughout (`ps-6`, `border-inline`… via the shared components); inline title inputs carry `aria-label`; the required checkbox is a real labelled `<input>` exposing its state; interactive targets stay at the `h-11` floor (no `size="sm"`).
- **Not done (conscious scope calls):** no drag/keyboard reordering (Story 2.3); no `lessonType` picker or per-type editors (2.4–2.8); "Preview as learner" and "Publish" render disabled (2.9 / 2.10); no per-Lesson settings panel beyond the one AC #6 toggle; no collapsed-module "N lessons" summary from the mockup (optional polish, omitted to stay minimal).

### File List

- `lib/modules/course-authoring/course.ts` (edit — `LESSON_TYPES` + `isLessonType`, `OutlineLesson`/`OutlineModule`/`CourseOutline` shared shapes, `MODULE_TITLE_MAX_LENGTH`/`LESSON_TITLE_MAX_LENGTH`, `parseOutlineTitle`)
- `lib/modules/course-authoring/schema.ts` (edit — `courseModule` + `lesson` tables, `lessonTypeEnum`, module/lesson relations, `(parent_id, position)` partial-unique indexes)
- `lib/modules/course-authoring/service.ts` (edit — `getCourseOutline`, `addModule`/`addLesson` (in-statement `max+1` + advisory lock + `insertAppending` retry), `updateModuleTitle`, `updateLessonTitle`, `setLessonRequired`, `getModuleCourseContext`, `getLessonCourseContext`, `ModuleRow`/`LessonRow`; re-exports the outline shapes)
- `lib/modules/course-authoring/actions.ts` (edit — `addModuleAction`, `addLessonAction`, `renameModuleAction`, `renameLessonAction`, `setLessonRequiredAction` + one shared `requireOwner(resolve, kind)` / `toErrorResult` helper)
- `drizzle/0007_fresh_forge.sql` (new — `module` + `lesson` tables, `lesson_type` enum, FKs, indexes; applied)
- `drizzle/0008_romantic_captain_america.sql` (new — `(course_id, position)` / `(module_id, position)` partial-unique indexes for the position race fix; also carries an external `course_category` enum `'rpa'` addition picked up by `db:generate`; applied)
- `drizzle/meta/0007_snapshot.json`, `drizzle/meta/0008_snapshot.json`, `drizzle/meta/_journal.json` (new/edit — drizzle-kit metadata)
- `app/[locale]/(instructor)/courses/[courseId]/page.tsx` (edit — renders the outline editor)
- `app/[locale]/(instructor)/courses/[courseId]/loading.tsx` (new — route-level skeleton)
- `components/ui/skeleton.tsx` (new — minimal pulse placeholder)
- `components/course/outline/outline-editor.tsx` (new — client; outline state, autosave status, header, empty state)
- `components/course/outline/outline-editor-skeleton.tsx` (new — client; AC #5 loading rows)
- `components/course/outline/module-block.tsx` (new — client; Module row + Add lesson)
- `components/course/outline/lesson-row.tsx` (new — client; Lesson title + meta + certificate toggle)
- `components/course/outline/use-outline-autosave.ts` (new — client; debounced per-field autosave engine)
- `lib/i18n/en.json` (edit — new `Course.outline.*`; removed unused `Course.builder.outlineComingSoon`)
- `lib/i18n/ar.json` (edit — new `Course.outline.*`; removed unused `Course.builder.outlineComingSoon`)

## Change Log

- 2026-09-03 — Story 2.2 implemented: `module` + `lesson` schema (nullable `lessonType`, `required` default true per AD-7, `removedAt` per AD-11, integer `position`) + migration `0007`; Course Authoring service outline read (`getCourseOutline`) and structural/autosave writes + ownership-context helpers; five instructor+owner-guarded Server Actions; the outline editor grown into `courses/[courseId]` (inline-editable Module/Lesson titles, add module/lesson, `loading.tsx` skeleton, debounced autosave with `aria-live` "Editing…"→"Saved.", certificate-eligibility toggle); `Course.outline.*` EN/AR i18n. `npm run build` / `tsc` / `lint` clean; migration `0007` applied; 32-assertion DB-layer verification all-pass. Status → review.
- 2026-09-03 — Addressed code-review findings (9 items, all resolved). **Autosave error channel:** removed `reportError()` from `useOutlineAutosave` — add/toggle failures now surface only via the inline `<FormMessage>`, so the header indicator can no longer get stuck in "error" with a dead Retry (findings #1, #4). **Empty-title save:** `handle{Module,Lesson}TitleChange` only `schedule()`s a save when the trimmed title is non-empty — clearing a field no longer strands the autosave indicator in an unrecoverable error (#2). **`position` race:** `addModule`/`addLesson` compute `max(position)+1` in-statement with a `pg_advisory_xact_lock` per parent, backed by new `(course_id, position)` / `(module_id, position)` partial-unique indexes (migration `0008`) and a bounded retry in `insertAppending` — concurrent adds can no longer produce duplicate positions (#3). **Add buttons:** every "Add module"/"Add lesson" button is disabled while any add is in flight (`addsBusy`), so a cross-module click can't silently no-op (#5). **`revalidatePath`:** dropped from `addModuleAction` (My Courses shows no module data) (#6). **Owner guards:** three `require*Owner` helpers collapsed to one `requireOwner(resolve, kind)` (#7). **Toggle race:** the certificate checkbox is disabled while its own write is in flight (`pendingToggle` set), making the optimistic rollback correct (#8). **Dead payload:** `rename{Module,Lesson}Action` now return `ActionResult<null>` and the misleading "AD-4 optimistic version" comment is corrected (#9). Re-ran `tsc` / `lint` / `build` clean; migration `0008` applied; 32-assertion DB regression + a concurrency stress script (cold 2–3 racers, warm 12-way burst) all-pass.
